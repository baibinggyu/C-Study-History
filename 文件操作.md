# C++ 文件输入输出操作完全指南

## 1. C++ 文件 I/O 基础

### 1.1 文件流概述

C++ 通过文件流类来处理文件输入输出操作，这些类定义在 `<fstream>` 头文件中。文件流类主要包括：

- `ifstream`：用于文件输入（读取）
- `ofstream`：用于文件输出（写入）
- `fstream`：用于文件输入和输出（读写）

这些类都是从 `iostream` 类派生而来的，因此它们继承了 `iostream` 的所有功能。

### 1.2 文件流类继承关系

```
ios_base
  ↑
ios
  ↑
istream           ostream
  ↑                 ↑
ifstream          ofstream
      \           /
        \       /
         fstream
```

### 1.3 文件打开模式

文件打开模式决定了文件如何被访问，常用的模式标志包括：

| 模式标志    | 描述                     |
| ----------- | ------------------------ |
| ios::in     | 以读取方式打开文件       |
| ios::out    | 以写入方式打开文件       |
| ios::binary | 以二进制模式打开文件     |
| ios::ate    | 打开文件后定位到文件末尾 |
| ios::app    | 以追加模式打开文件       |
| ios::trunc  | 如果文件存在则截断文件   |

这些标志可以通过位或操作符 `|` 组合使用。

## 2. 文件基本操作

### 2.1 打开文件

#### 2.1.1 使用构造函数打开文件

```cpp
#include <fstream>

// 写入文件
std::ofstream outFile("example.txt", std::ios::out);

// 读取文件
std::ifstream inFile("example.txt", std::ios::in);

// 读写文件
std::fstream ioFile("example.txt", std::ios::in | std::ios::out);
```

#### 2.1.2 使用 open() 方法打开文件

```cpp
std::fstream file;
file.open("example.txt", std::ios::in | std::ios::out);
```

### 2.2 检查文件是否成功打开

```cpp
if (!file.is_open()) {
    std::cerr << "无法打开文件!" << std::endl;
    return 1;
}
```

或者使用重载的 `!` 操作符：

```cpp
if (!file) {
    std::cerr << "无法打开文件!" << std::endl;
    return 1;
}
```

### 2.3 关闭文件

```cpp
file.close();
```

虽然文件流对象在销毁时会自动关闭文件，但显式关闭文件是一个好习惯。

### 2.4 文件状态检查

| 方法    | 描述                   |
| ------- | ---------------------- |
| good()  | 检查流是否处于良好状态 |
| eof()   | 检查是否到达文件末尾   |
| fail()  | 检查是否发生非致命错误 |
| bad()   | 检查是否发生致命错误   |
| clear() | 重置错误状态标志       |

## 3. 文本文件操作

### 3.1 写入文本文件

#### 3.1.1 使用插入运算符 <<

```cpp
std::ofstream outFile("example.txt");
if (outFile) {
    outFile << "第一行文本" << std::endl;
    outFile << "第二行文本" << std::endl;
    outFile << 123 << " " << 3.14 << std::endl;
}
```

#### 3.1.2 使用 put() 写入单个字符

```cpp
outFile.put('A');
outFile.put('\n');
```

#### 3.1.3 使用 write() 写入字符串

```cpp
const char* str = "Hello, World!";
outFile.write(str, strlen(str));
```

### 3.2 读取文本文件

#### 3.2.1 使用提取运算符 >>

```cpp
std::ifstream inFile("example.txt");
std::string word;
int num;
double pi;

if (inFile) {
    inFile >> word;  // 读取第一个单词
    inFile >> num >> pi;  // 读取数字
}
```

注意：`>>` 操作符会跳过空白字符（空格、制表符、换行符）。

#### 3.2.2 使用 get() 读取单个字符

```cpp
char ch;
while (inFile.get(ch)) {
    std::cout << ch;
}
```

#### 3.2.3 使用 getline() 读取整行

```cpp
std::string line;
while (std::getline(inFile, line)) {
    std::cout << line << std::endl;
}
```

#### 3.2.4 使用 read() 读取数据块

```cpp
char buffer[100];
inFile.read(buffer, sizeof(buffer));
std::streamsize bytesRead = inFile.gcount();
```

## 4. 二进制文件操作

### 4.1 二进制文件与文本文件的区别

1. 文本文件：
   - 以ASCII码形式存储数据
   - 适合存储可读文本
   - 可能会进行换行符转换（\n ↔ \r\n）

2. 二进制文件：
   - 以原始字节形式存储数据
   - 适合存储任意类型数据
   - 不进行任何转换

### 4.2 写入二进制文件

```cpp
struct Person {
    char name[50];
    int age;
    double height;
};

Person p = {"张三", 25, 175.5};

std::ofstream outFile("person.dat", std::ios::binary);
if (outFile) {
    outFile.write(reinterpret_cast<char*>(&p), sizeof(Person));
}
```

### 4.3 读取二进制文件

```cpp
Person p;
std::ifstream inFile("person.dat", std::ios::binary);
if (inFile) {
    inFile.read(reinterpret_cast<char*>(&p), sizeof(Person));
    std::cout << "姓名: " << p.name 
              << ", 年龄: " << p.age 
              << ", 身高: " << p.height << std::endl;
}
```

### 4.4 二进制文件操作注意事项

1. 平台兼容性问题：
   - 不同平台可能有不同的字节序（大端/小端）
   - 结构体可能有不同的对齐方式

2. 解决方案：
   - 使用序列化库（如 Protocol Buffers、Boost.Serialization）
   - 手动处理字节序和对齐

## 5. 文件定位操作

### 5.1 获取当前位置

```cpp
// 获取当前读取位置
std::streampos readPos = inFile.tellg();

// 获取当前写入位置
std::streampos writePos = outFile.tellp();
```

### 5.2 设置位置

```cpp
// 设置读取位置
inFile.seekg(offset, origin);

// 设置写入位置
outFile.seekp(offset, origin);
```

其中，origin 可以是：
- `std::ios::beg`：文件开头
- `std::ios::cur`：当前位置
- `std::ios::end`：文件末尾

### 5.3 示例：随机访问二进制文件

```cpp
struct Record {
    int id;
    char data[100];
};

// 写入10条记录
std::fstream file("records.dat", std::ios::binary | std::ios::in | std::ios::out);
for (int i = 0; i < 10; ++i) {
    Record r = {i, "记录数据"};
    file.write(reinterpret_cast<char*>(&r), sizeof(Record));
}

// 读取第5条记录
Record fifth;
file.seekg(4 * sizeof(Record), std::ios::beg);
file.read(reinterpret_cast<char*>(&fifth), sizeof(Record));
```

## 6. 文件系统操作（C++17）

C++17 引入了 `<filesystem>` 库，提供了更高级的文件系统操作功能。

### 6.1 常用操作

```cpp
#include <filesystem>
namespace fs = std::filesystem;

// 检查文件是否存在
if (fs::exists("example.txt")) {
    // 获取文件大小
    auto size = fs::file_size("example.txt");
    
    // 获取最后修改时间
    auto time = fs::last_write_time("example.txt");
    
    // 重命名文件
    fs::rename("old.txt", "new.txt");
    
    // 删除文件
    fs::remove("example.txt");
    
    // 创建目录
    fs::create_directory("new_dir");
}
```

### 6.2 遍历目录

```cpp
for (const auto& entry : fs::directory_iterator(".")) {
    std::cout << entry.path() << std::endl;
    if (entry.is_directory()) {
        std::cout << " (目录)" << std::endl;
    } else if (entry.is_regular_file()) {
        std::cout << " (文件, 大小: " << entry.file_size() << " bytes)" << std::endl;
    }
}
```

## 7. 高级文件操作技巧

### 7.1 内存映射文件

内存映射文件可以提高大文件的访问效率：

```cpp
#include <sys/mman.h>
#include <fcntl.h>
#include <unistd.h>

int fd = open("largefile.bin", O_RDONLY);
off_t size = lseek(fd, 0, SEEK_END);
void* mapped = mmap(nullptr, size, PROT_READ, MAP_PRIVATE, fd, 0);

// 使用映射的内存
char* data = static_cast<char*>(mapped);
for (off_t i = 0; i < size; ++i) {
    process(data[i]);
}

// 解除映射
munmap(mapped, size);
close(fd);
```

注意：Windows 平台有类似的 API `CreateFileMapping` 和 `MapViewOfFile`。

### 7.2 文件锁定

实现文件锁定以防止并发访问冲突：

```cpp
#include <sys/file.h>

int fd = open("data.txt", O_RDWR);
if (flock(fd, LOCK_EX) == 0) {  // 获取排他锁
    // 执行文件操作
    flock(fd, LOCK_UN);  // 释放锁
}
close(fd);
```

### 7.3 临时文件

创建安全的临时文件：

```cpp
#include <cstdio>

char tempName[] = "/tmp/tempfileXXXXXX";
int fd = mkstemp(tempName);
if (fd != -1) {
    FILE* tempFile = fdopen(fd, "w+");
    // 使用临时文件
    fclose(tempFile);
    unlink(tempName);  // 删除临时文件
}
```

## 8. 错误处理与异常

### 8.1 流状态检查

```cpp
std::ifstream file("data.txt");
if (!file) {
    // 检查具体错误
    if (file.rdstate() & std::ios::failbit) {
        std::cerr << "逻辑错误" << std::endl;
    }
    if (file.rdstate() & std::ios::badbit) {
        std::cerr << "不可恢复的错误" << std::endl;
    }
    if (file.rdstate() & std::ios::eofbit) {
        std::cerr << "到达文件末尾" << std::endl;
    }
}
```

### 8.2 使用异常

可以配置流在特定错误时抛出异常：

```cpp
std::ifstream file;
file.exceptions(std::ios::failbit | std::ios::badbit);

try {
    file.open("data.txt");
    // 文件操作
} catch (const std::ios_base::failure& e) {
    std::cerr << "文件操作失败: " << e.what() << std::endl;
}
```

## 9. 性能优化技巧

### 9.1 缓冲优化

```cpp
// 设置更大的缓冲区
char buffer[8192];
std::ifstream file("largefile.txt");
file.rdbuf()->pubsetbuf(buffer, sizeof(buffer));
```

### 9.2 减少I/O操作

```cpp
// 批量读取数据
const int BUFFER_SIZE = 4096;
char buffer[BUFFER_SIZE];
while (file.read(buffer, BUFFER_SIZE)) {
    processBuffer(buffer, file.gcount());
}
```

### 9.3 使用内存映射文件

对于超大文件，内存映射通常是最快的方式（见7.1节）。

## 10. 跨平台注意事项

1. 路径分隔符：
   - Windows 使用 `\`
   - Unix-like 系统使用 `/`
   - C++17 的 `filesystem::path` 可以自动处理

2. 文本文件换行符：
   - Windows: `\r\n`
   - Unix: `\n`
   - Mac OS (旧版): `\r`

3. 文件权限：
   - Unix-like 系统有更复杂的权限系统
   - Windows 主要依赖ACL

4. 文件名编码：
   - 在Windows上可能需要处理UTF-16
   - 在Unix-like系统上通常是UTF-8

## 11. 实际应用示例

### 11.1 配置文件读写

```cpp
#include <map>
#include <sstream>

std::map<std::string, std::string> readConfig(const std::string& filename) {
    std::map<std::string, std::string> config;
    std::ifstream file(filename);
    
    std::string line;
    while (std::getline(file, line)) {
        std::istringstream iss(line);
        std::string key, value;
        if (std::getline(iss, key, '=') && std::getline(iss, value)) {
            config[key] = value;
        }
    }
    
    return config;
}

void writeConfig(const std::string& filename, 
                const std::map<std::string, std::string>& config) {
    std::ofstream file(filename);
    for (const auto& pair : config) {
        file << pair.first << "=" << pair.second << "\n";
    }
}
```

### 11.2 日志系统实现

```cpp
class Logger {
public:
    enum Level { DEBUG, INFO, WARNING, ERROR };
    
    Logger(const std::string& filename) : file(filename, std::ios::app) {}
    
    void log(Level level, const std::string& message) {
        if (!file) return;
        
        auto now = std::chrono::system_clock::now();
        auto time = std::chrono::system_clock::to_time_t(now);
        
        file << std::put_time(std::localtime(&time), "%Y-%m-%d %H:%M:%S") << " ["
             << levelToString(level) << "] " << message << std::endl;
    }
    
    ~Logger() {
        if (file) file.close();
    }
    
private:
    std::ofstream file;
    
    std::string levelToString(Level level) {
        static const char* levels[] = {"DEBUG", "INFO", "WARNING", "ERROR"};
        return levels[level];
    }
};
```

## 12. 总结

C++ 提供了丰富的文件操作功能，从基本的文本文件读写到高级的二进
<!-- @import "[TOC]" {cmd="toc" depthFrom=1 depthTo=6 orderedList=false} -->
制文件操作、文件系统管理和性能优化。关键点包括：

1. 理解文件流类（`ifstream`, `ofstream`, `fstream`）的使用
2. 掌握文本和二进制文件的操作区别
3. 熟练使用文件定位操作实现随机访问
4. 了解C++17的文件系统库
5. 掌握错误处理和性能优化技巧

在实际开发中，应根据具体需求选择合适的文件操作方式，并注意跨平台兼容性和异常处理。